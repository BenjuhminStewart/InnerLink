<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/colorNavBar</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dark application theme. -->
    <style name="DarkAppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/DarkThemecolorNavBar</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/DarkThemecolorNavBar</item>
        <item name="colorPrimary">@color/DarkThemecolorNavBar</item>
        <item name="colorPrimaryDark">@color/DarkThemecolorNavBar</item>
        <item name="colorAccent">@color/DarkThemecolorNavBar</item>
    </style>
</resources>
